

SELECT * FROM zipcodes WHERE city, state =  city, state OR city, state, county = city, state, county AND (zip IS NULL);


* Write a query that locates the correct values and updates the `FALSE` and `NULL` values.
